# 🔐 CryptoMsg (پیام‌رسان سری)

![License](https://img.shields.io/badge/license-MIT-blue.svg)
![Version](https://img.shields.io/badge/version-4.0-green.svg)
![Security](https://img.shields.io/badge/Security-AES--GCM-red.svg)
![PWA](https://img.shields.io/badge/PWA-Ready-orange.svg)

## ⚠️ هشدار مهم (نسخه ۴.۰)
در این نسخه، هسته امنیتی برنامه به طور کامل بازنویسی شده است تا استانداردهای نظامی را پاس کند. به همین دلیل، **پیام‌هایی که با نسخه‌های قدیمی (۱ تا ۳) رمز شده‌اند، با این نسخه باز نمی‌شوند.**

## 🔒 معرفی
**CryptoMsg** یک ابزار امنیتی متن‌باز و آفلاین است که پیام‌های متنی شما را با استفاده از **استاندارد Web Crypto API** (موتور رمزنگاری داخلی مرورگر) قفل می‌کند. برخلاف نسخه قبل، در این نسخه از الگوریتم **AES-GCM** استفاده شده که علاوه بر محرمانگی، **تضمین می‌کند پیام در طول مسیر دستکاری نشده باشد.**

🔗 **[اجرای آنلاین (Cloudflare Pages - نسخه پایدار)](https://cryptomsg.pages.dev/)**
🔗 **[اجرای آنلاین (GitHub Pages)](https://443a.github.io/CryptoMsg/)**

## ✨ ویژگی‌های کلیدی (نسخه ۴.۰)

### 🛡️ امنیت سطح بالا (Major Upgrade)
* **حذف کتابخانه‌های جانبی:** حذف کامل `CryptoJS`. اکنون کدها مستقیماً با هسته مرورگر (Native Code) اجرا می‌شوند که بسیار سریع‌تر و امن‌تر است.
* **الگوریتم AES-GCM:** استفاده از حالت GCM به جای CBC. اگر حتی یک بیت از پیام رمز شده توسط هکر یا سرور واسط تغییر کند، رمزگشایی با خطا متوقف می‌شود (Tamper Proof).
* **مشتق‌سازی کلید (PBKDF2):** رمز عبور شما با یک نمک (Salt) تصادفی ترکیب شده و **۱۰۰,۰۰۰ بار** درهم‌ریخته می‌شود تا حملات Brute-force عملاً غیرممکن شوند.

### 🕵️ پنهان‌نگاری (Steganography)
* **متن نامرئی:** مخفی کردن پیام رمز شده در دل یک متن عادی (با استفاده از کاراکترهای Zero-Width).
* **ضد فیلتر:** تبدیل پیام به حروف روسی، چینی یا جملات فارسی بی‌معنی برای عبور از فیلترهای هوشمند.

### 📱 دسترسی پذیری
* **وب‌اپلیکیشن (PWA):** قابل نصب روی تمام سیستم‌عامل‌ها.
* **کاملاً آفلاین:** پس از لود شدن، بدون نیاز به اینترنت کار می‌کند (مناسب برای شرایط Air-Gap).

## 📊 مشخصات فنی (Technical Specs)

برای شفافیت کامل، مشخصات دقیق الگوریتم‌های استفاده شده در نسخه ۴:

| ویژگی | استاندارد استفاده شده | توضیحات |
| :--- | :--- | :--- |
| **Encryption** | `AES-GCM` (256-bit) | رمزنگاری با تایید اصالت (Authenticated Encryption) |
| **Key Derivation** | `PBKDF2` | با الگوریتم HMAC-SHA256 |
| **Iterations** | `100,000` | تعداد تکرار هش برای کند کردن حملات آفلاین |
| **Randomness** | `CSPRNG` | استفاده از `crypto.getRandomValues` برای تولید Salt و IV |

## 🚀 نحوه استفاده

1.  **ارسال:** متن و رمز عبور را بنویسید. دکمه "تولید پیام امن" را بزنید. خروجی کپی شده را برای گیرنده بفرستید.
2.  **دریافت:** متن رمز شده را در تب "دریافت" وارد کنید، رمز عبور را بزنید تا پیام باز شود.

## 🛠️ نصب و توسعه

این پروژه هیچ وابستگی (Dependency) خارجی ندارد. فایل‌ها را دانلود و اجرا کنید.

```bash
git clone [https://github.com/443a/CryptoMsg.git](https://github.com/443a/CryptoMsg.git)
cd CryptoMsg
# فایل index.html را در مرورگر باز کنید